# Liver_Tumor_Segmentation_cascade_post-processing
implements a cascade (two-stage) deep learning pipeline for segmenting the liver and liver tumors from 3D CT scans. The approach uses the LiTS (Liver Tumor Segmentation) dataset and applies two separate U-Net models in sequence.
